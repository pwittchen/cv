name: generate cv/resume
on:
  push:
    branches:
      - master
  inputs:
    input-file: cv.tex
jobs:
  build:
    name: generate
    runs-on: alpine-latest
    steps:
      - name: generate
        uses: marcomicera/xelatex-action@v0.6
